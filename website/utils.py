#from .models import AuditLogs
#from . import db

#def log_action(action, user_id=None, details=None):
    #log = AuditLogs(
    #    action=action,
    #    user_id=user_id,
    #    details=details,        
    #)
    #db.session.add(log)
    #db.session.commit()
